<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.health">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo3"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".FortisuroActivity"></activity>
        <activity android:name=".NimhansActivity" />
        <activity android:name=".KidwaiActivity" />
        <activity android:name=".JaydevaActivity" />
        <activity android:name=".HlistActivity" />
        <activity android:name=".symptomActivity" />
        <activity android:name=".sapActivity" />
        <activity android:name=".FortisActivity" />
        <activity android:name=".ApolloActivity" />
        <activity android:name=".MsrActivity" />
        <activity android:name=".MainActivity2" />
        <activity android:name=".searchActivity" />
        <activity android:name=".registerActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>